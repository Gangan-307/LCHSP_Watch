# LVGL 首屏优先临时修复方案

## 说明

本方案内容已并入主排查文档：

- `d:\iotproject\lcHspEc800m\lcHsp\lvgl\lvgl_v8_test\debug-lvgl-screen-black.md`

为了避免两份文档长期并行维护造成结论不一致，本文件仅保留为中文索引和摘要，后续以主排查文档为准。

## 本方案摘要

- 当前问题的直接触发点位于触摸输入路径，而不是 LCD 基础初始化路径。
- 本阶段的目标不是一次性修复全部触摸驱动问题，而是先保证 SquareLine UI 可以在屏幕上显示。
- 临时策略是降级或移除高风险的手势释放等待逻辑，先验证显示链路。
- 触摸驱动的完整修复，包括 `tp_init`、`more_data_lock`、`isr_sem` 和断言处理链路，属于下一阶段工作。

## 查看建议

- 若要查看完整背景、日志、根因判断、临时修复方案、验证步骤和后续计划，请直接阅读主文档。
